package UserList;

/**
 * Обязательный член класса Message.
 * Используется для того, чтобы определить какого типа сообщение пришло/отправлено
 */
public enum MsgType {

    authentication, userUpdate, textMsg, registration,
}
